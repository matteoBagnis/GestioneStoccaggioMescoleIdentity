@model IEnumerable<MvcMovie.Models.Mescola>

@{
    ViewData["Title"] = "Gestione Mescole";
}

<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewData["Title"]</title>
    <style>
        /* Stili CSS */
        body {
            font-family: Arial, sans-serif;
            background-color: #f0f0f0;
            color: #333;
        }

        .container {
            max-width: 100%; /* Modifica qui */
            margin: 0 auto;
            padding: 20px;
            background-color: #fff;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        h1 {
            color: #333;
            margin-bottom: 40px;
            text-align: center;
        }

        .btn {
            text-align: center;
            margin-bottom: 20px;
        }

        .table {
            border-collapse: collapse;
            width: 100%;
        }

            .table th, .table td {
                padding: 10px;
                text-align: left;
                border-bottom: 1px solid #ddd;
            }

            .table th {
                background-color: #007bff;
                color: #fff;
            }

            .table tr:nth-child(even) {
                background-color: #f8f9fa;
            }

            .table a {
                color: #007bff;
                text-decoration: none;
                margin-right: 5px;
            }

                .table a.delete {
                    color: #dc3545;
                }

    </style>
</head>
<body>
    <div class="container">
        <h1>@ViewData["Title"]</h1>

        <div style="text-align: center; margin-bottom: 20px;">
            <a asp-action="Create" class="btn btn-primary" style="text-decoration: none;">➕ Aggiungi una mescola</a>
            <br> <!-- Aggiunto il tag <br> qui -->
        </div>

        <table class="table" style="margin-top: 20px;">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Descrizione</th>
                    <th>Data Produzione</th>
                    <th>Prezzo</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@Html.DisplayFor(modelItem => item.Id)</td>
                        <td>@Html.DisplayFor(modelItem => item.Descrizione)</td>
                        <td>@Html.DisplayFor(modelItem => item.DataProduzione)</td>
                        <td>@Html.DisplayFor(modelItem => item.Prezzo)</td>
                        <td>
                            <a asp-action="Edit" asp-route-id="@item.Id">Modifica</a> |
                            <a asp-action="Details" asp-route-id="@item.Id">Dettagli</a> |
                            <a asp-action="Delete" asp-route-id="@item.Id" class="delete">Cancella</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>

        <div style="text-align: center; margin-top: 20px;">
            <a href="https://localhost:44361" class="btn btn-secondary" style="text-decoration: none;">⬅️ Torna indietro</a>
        </div>
    </div>
</body>
</html>
